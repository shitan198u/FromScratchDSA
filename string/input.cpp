#include <bits/stdc++.h>
using namespace std; 

int main()
{
    string s,str;
    cin>>s;
    cout<<s<<endl;

    //it will terminate at the newline space or tab so to take line as input
    getline(cin,str);
    cout<<str<<endl;

}